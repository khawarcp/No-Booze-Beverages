<!-- Future -->
<section class="story-section future-section space-left">
    <div class="story-row flex jc-sb ai-center">
        <div class="present-copy">
            <span> {{ section.settings.subheading }} </span>
            <h2> {{ section.settings.heading }} </h2>
           {{ section.settings.info }}
        </div>
        <div class="story-image">
            {%  render 'responsive-image', image: section.settings.image %}             
        </div>
    </div>
</section>

{% schema %}
    {
        "name": "Future",
        "settings": [
            {
                "type": "text",
                "id": "heading",
                "label": "Heading"
            },
            {
                "type": "text",
                "id": "subheading",
                "label": "Subheading"
            },
            {
                "type": "richtext",
                "id": "info",
                "label": "Description"
            },            
            {
                "type": "image_picker",
                "id": "image",
                "label": "Image"
            }
        ]
    }
{% endschema %}